apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
data:
  root-password: {{ .Values.mysqlRootPassword | b64enc }}
  user-password: {{ .Values.mysqlPassword | b64enc }}
  readonly-password: {{ .Values.mysqlReadOnlyPassword | b64enc }}